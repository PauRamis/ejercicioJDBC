package Menus;

public class Menu {

    public boolean running;
    protected String title;

    public Menu() {
        running = true;
        this.title = title;


        public void start() {
            while (running) {
                // imprimir titulo del menu
                System.out.println(title);
                // imprimir las opciones

                // pedir al usuario la opcion que quiere
                // ejecutar la opcion elejida
            }
        }
    }
}
